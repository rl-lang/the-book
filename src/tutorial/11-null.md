# 11. Null

`null` represents the absence of a value; functions with no return implicitly return null

```rl
dec int x = null
println(x) // null

fn do_nothing() {
    // implicitly returns null
}
```
